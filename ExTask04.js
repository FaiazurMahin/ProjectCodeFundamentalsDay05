// Create an array of numbers.
// Use map() to create a new array where each element is the square of the original element.

const num=[1,2,3,4,5,6,7,8,9];

let squarenum = num.map(X=>X*X);
console.log(squarenum);